// Committed project config. Optionally includes a developer-local override
// file (Developer.xcconfig) for personal signing settings such as
// DEVELOPMENT_TEAM. The `?` makes the include silent when the file is absent,
// so fresh clones build without needing any local setup.
#include? "Developer.xcconfig"

// Version numbers. MARKETING_VERSION is bumped automatically by
// .github/workflows/version-bump.yml based on merged PR titles.
// CURRENT_PROJECT_VERSION is overwritten at Xcode Cloud build time by
// ci_scripts/ci_pre_xcodebuild.sh using $CI_BUILD_NUMBER.
MARKETING_VERSION = 1.0.0
CURRENT_PROJECT_VERSION = 1

// Promote Swift warnings to errors so unused vars, deprecations, etc. fail
// the build instead of accumulating. Applies to all targets via project base.
SWIFT_TREAT_WARNINGS_AS_ERRORS = YES

// Shared transit API base URL for iPhone and Watch targets.
// Leave blank to use direct 511.org calls; set to your Cloudflare Worker URL
// (for example: https://sftransitwatch.your-subdomain.workers.dev).
TRANSIT_API_BASE_URL =

// App-side shared secret presented as X-App-Token to the Cloudflare Worker.
// Required when TRANSIT_API_BASE_URL points at the worker; ignored in direct-511 mode.
// Set in Developer.xcconfig (gitignored).
APP_TOKEN = $(APP_TOKEN)

// Base URL of the worker that accepts POST /log telemetry events.
// Defaults to the production worker; override in Developer.xcconfig for local testing.
TELEMETRY_BASE_URL = https://sftransitwatch.rusty-cloudflare.workers.dev
