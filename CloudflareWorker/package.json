{
	"name": "sftransitwatch",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"prepare:wrangler": "node ./scripts/set-kv-id.mjs",
		"deploy": "npm run prepare:wrangler && wrangler deploy",
		"dev": "npm run prepare:wrangler && wrangler dev",
		"start": "npm run prepare:wrangler && wrangler dev",
		"cf-typegen": "npm run prepare:wrangler && wrangler types"
	},
	"devDependencies": {
		"@types/node": "^25.6.0",
		"typescript": "^5.5.2",
		"wrangler": "^4.83.0"
	}
}
